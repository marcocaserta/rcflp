To run the code, the cplex library must be linked first. Next, use:

> python cflp.py -t (type) -i (instance)

type: the type of model to be used. Currently, these are the options:
 -t 1 : deterministic MS-CFLP (OR Library instances) 
 -t 2 : deterministic MS-CFLP (Avella instances) 
 -t 4 : robust MS-CFLP (OR Library instances)

instance: the instance to be used. Currently, the OR Library instance are
          provided. They are under data/orLibrary.
